import 'package:flutter/material.dart';
import 'manage_prescription.dart';

class ManagePrescriptionScreen extends StatelessWidget {
  const ManagePrescriptionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ManagePrescription();
  }
}
