import 'package:flutter/material.dart';
import 'components/live_prescription.dart';

class LivePrescriptionScreen extends StatelessWidget {
  const LivePrescriptionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const LivePrescription();
  }
}
